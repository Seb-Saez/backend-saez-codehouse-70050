<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Seb Saez | API</title>
    {{!-- no hay forma de importar el css, asique lo solucione asi --}}
    <style>
        :root {
    --background: #f0f0f0;
    --color1: #007bff;
    --color2: #28a745;
    --text-color: #333;
}

body {
    background-color: var(--background);
    font-family: Arial, sans-serif;
    color: var(--text-color);
    margin: 0;
    padding: 0;
}

.cardContainer {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 20px;
    padding: 20px;
    max-width: 1200px;
    margin: 0 auto;
}

.card {
    background-color: #fff;
    border: 1px solid #ddd;
    border-radius: 10px;
    padding: 20px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    transition: transform 0.2s, box-shadow 0.2s;
}

.card:hover {
    transform: translateY(-10px);
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
}

.card h3 {
    margin: 0 0 10px;
    color: var(--color1);
}

.card p {
    text-transform: capitalize;
    margin: 8px 0;
}

.card p strong {
    color: var(--color2);
}


    </style>
</head>
<body>
    {{{body}}}
</body>
</html>
